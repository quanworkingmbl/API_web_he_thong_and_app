# ============================================================
# CMS API — Dependencies
# Pin các package core, để flexible cho google-genai & AI libs
# ============================================================

# --- Web Framework ---
fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0

# --- Database ---
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# --- Pydantic (>= 2.9 required by google-genai) ---
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.1.0

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
argon2-cffi==23.1.0

# --- Utils ---
python-dotenv==1.0.0
python-multipart==0.0.6
email-validator==2.1.0

# --- HTTP Client (>= 0.28.1 required by google-genai) ---
httpx>=0.28.1,<1.0.0

# --- Google AI (chỉ cần google-genai, không cần aiplatform SDK) ---
google-genai>=1.66.0

# --- Google Cloud Storage (thay thế AWS S3) ---
google-cloud-storage>=2.14.0

# --- Scheduler (Cron jobs: release wallet reserves tự động) ---
apscheduler>=3.10.0,<4.0.0

# --- Email OTP (Resend) ---
resend>=0.8.0

# --- Redis (Upstash OTP cache) ---
redis>=5.0.0

# --- Firebase Admin (Firestore chat + FCM push notification) ---
firebase-admin>=6.5.0
